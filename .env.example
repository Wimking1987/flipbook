# Optional: Vercel Blob — set in Vercel (or locally when testing against Blob).
# Without this, uploads use `.data/flipbook/` (OK for local dev only; not persistent on serverless).
BLOB_READ_WRITE_TOKEN=
